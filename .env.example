# Application
PORT=3000
NODE_ENV=development

# Database - Updated port to match docker-compose
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/email_service
DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=email_service

# Redis (for BullMQ) - Updated port to match docker-compose
REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_PASSWORD=

# SMTP Configuration (Mailhog for development)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Gmail OAuth2 (optional)
GMAIL_CLIENT_ID=your_client_id
GMAIL_CLIENT_SECRET=your_client_secret
GMAIL_REFRESH_TOKEN=your_refresh_token

# Application Settings
APP_NAME=DN Secure Email Module
APP_URL=http://localhost:3000
DEFAULT_FROM_EMAIL=noreply@example.com
