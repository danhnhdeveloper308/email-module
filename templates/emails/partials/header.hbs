<header style="background-color: #f8f9fa; padding: 20px; text-align: center; border-bottom: 1px solid #e9ecef;">
  {{#if logoUrl}}
  <img src="{{logoUrl}}" alt="{{appName}} Logo" style="max-height: 50px;">
  {{else}}
  <h2 style="margin: 0; color: #333;">{{appName}}</h2>
  {{/if}}
</header>